import React from 'react';

// Archived: ApprovalList (deprecated)
// Original file moved to archive because approval workflow was removed.
const ApprovalList = () => null;

export default ApprovalList;
